<Window x:Class="WeChatPublisher.Views.McpConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MCP 资源配置" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="DgResources" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionChanged="DgResources_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="类型" Binding="{Binding ResourceType}" Width="80"/>
                <DataGridTextColumn Header="路径" Binding="{Binding Path}" Width="*"/>
                <DataGridTextColumn Header="文件过滤" Binding="{Binding FileFilter}" Width="80"/>
                <DataGridTemplateColumn Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="编辑" Width="60" Tag="{Binding Id}"
                                        Click="BtnEdit_Click"/>
                                <Button Content="删除" Width="60" Tag="{Binding Id}" Margin="4,0,0,0"
                                        Style="{StaticResource DangerButton}"
                                        Click="BtnDelete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="1" Header="添加/编辑资源" Padding="8" Margin="0,8,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/><ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="名称:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtResName" Margin="0,2"/>

                <Label Grid.Row="1" Grid.Column="0" Content="类型:"/>
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CmbResType" Margin="0,2">
                    <ComboBoxItem Content="📁 本地文件夹" Tag="LocalFolder"/>
                    <ComboBoxItem Content="🖥 网络共享" Tag="NetworkShare"/>
                    <ComboBoxItem Content="🌐 网页URL" Tag="WebUrl"/>
                    <ComboBoxItem Content="📡 RSS订阅" Tag="RssFeed"/>
                </ComboBox>

                <Label Grid.Row="2" Grid.Column="0" Content="路径:"/>
                <Grid Grid.Row="2" Grid.Column="1" Margin="0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtResPath" Grid.Column="0"/>
                    <Button Grid.Column="1" Content="浏览..." Width="75" Click="BtnBrowseResPath_Click"/>
                </Grid>

                <Label Grid.Row="3" Grid.Column="0" Content="文件过滤:"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtResFilter" Text="*.*" Margin="0,2"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnAddSave" Content="添加/保存" Width="90"
                    Style="{StaticResource PrimaryButton}" Click="BtnAddSave_Click"/>
            <Button Content="关闭" Width="70" Margin="10,0,0,0" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
